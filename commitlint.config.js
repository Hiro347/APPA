module.exports = {
  extends: ['@commitlint/config-conventional'],
  rules: {
    // Override rules if needed, e.g., to allow uppercase subjects
    // 'subject-case': [2, 'never', ['start-case', 'pascal-case']]
  }
};
